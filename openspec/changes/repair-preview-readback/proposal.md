# Why

The public `/preview/` page already demonstrates a two-candidate database repair comparison, but it is a standalone static artifact. The final incident archive therefore cannot answer which candidate was measured, why one passed and the other was rejected, or how that evidence affected the human approval gate.

This change makes controlled repair preview a first-class, incident-bound stage in the main PG fault scenario and exposes its sanitized readback through the existing `opskeeper-teamharness` Archive view.

# What Changes

- Record repair preview runs and candidate evaluations against the tenant and business incident ID, including workload/seed fingerprints, isolation boundary, baseline metrics, candidate metrics, result consistency, query latency, throughput, write impact, and pass/fail decision.
- Execute candidates in isolated preview-pg branches created from a deterministic fixture seed. Runtime faults such as connection-pool exhaustion or lock waits are generated by controlled load; the system explicitly does not claim to copy the original instance's active sessions.
- Add two final-demo candidates to the PG pool-exhaustion main flow:
  - Candidate A adjusts pool capacity and passes fixed-load correctness, latency, throughput, and recovery checks, then becomes eligible for HITL.
  - Candidate B performs a risky pool reset/session-clear action, fails controlled business probes or consistency/write-impact checks, and is rejected before formal change.
- Enforce preview evidence as a precondition for risky formal-change proposals. Preview PASS does not approve automatically; approval remains an explicit human decision.
- Extend the Manager incident archive API with sanitized `repair_previews` and append compact preview events to the incident timeline.
- Render the candidate comparison and boundary explanation directly in the TeamHarness Archive tab. Keep the existing preview URL as a secondary deep link, not the source of truth.
- Keep AgentTeams and Dashboard host code unchanged.

# Capabilities

## New Capabilities

- `repair-preview-readback`: Record controlled, incident-bound repair preview evaluations and project their approval/rejection evidence into Manager APIs and the TeamHarness Archive view.

## Modified Capabilities

- None.

# Impact

- Manager incident/control-plane storage, migration, preview service, archive API, and HITL proposal validation.
- Preview-pg deployment, deterministic seed/workload runner, and final-demo fixtures.
- `opskeeper-teamharness` Dashboard normalization and Archive UI.
- E2E demo materials and public deployment validation.
- No AgentTeams Controller, Dashboard host, or external PolarDB dependency in this change.
